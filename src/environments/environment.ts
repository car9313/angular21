/**
 * Development environment (default).
 * Only NON-SENSITIVE build-time values belong here.
 * Anything runtime-configurable (server URLs, feature flags) goes into
 * the /assets/config.{env}.json files read by ConfigService.
 */
export const environment = {
    env: 'development'
};
