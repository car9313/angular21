/**
 * Production environment.
 * Selected via fileReplacements in angular.json (build:production).
 * Keep ONLY non-sensitive build-time values here.
 */
export const environment = {
    env: 'production'
};